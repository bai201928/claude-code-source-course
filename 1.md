可以。**自然语言提示词都应保存为 `.md` 文件**。但不要把所有规则都塞进第一次对Codex输入的聊天消息里：

* `AGENTS.md`：Codex自动读取的项目级长期规则；
* `CLAUDE.md`：Claude Code自动读取的审查环境规则；
* `orchestrator/prompts/*.md`：完整的主控、事实审查、教学审查提示词；
* `schemas/*.json`：纯JSON输出约束；
* `config/*.yaml`：课程参数；
* `scripts/*.sh`或`.ps1`：启动Codex和Claude Code的脚本。

Codex会按工作目录层级自动读取适用的`AGENTS.md`；Claude Code也会从当前目录向上读取`CLAUDE.md`。因此，这两个特殊文件应保持简洁，详细提示词放在单独的`.md`文件中。([GitHub][1])

---

# 一、不要让Codex直接打开“快照目录”

正确做法是：

```text
Codex打开课程工程根目录
课程工程根目录中包含只读的source-snapshot
```

不要这样：

```text
claude-code-snapshot/
└── 在这里直接启动Codex
```

应该这样：

```text
claude-code-course/
├── AGENTS.md
├── source-snapshot/       # 只读快照
├── input/
├── orchestrator/
├── curriculum/
└── mini-agent-harness/
```

启动位置：

```bash
cd ~/work/claude-code-course
codex --sandbox workspace-write
```

Codex的`workspace-write`模式允许它修改当前工程中的课程、脚本和实验文件；快照目录再通过操作系统权限单独设为只读。Codex CLI支持`read-only`、`workspace-write`和`danger-full-access`等沙箱策略；本项目不要使用`danger-full-access`。([GitHub][2])

---

# 二、推荐的最终目录

建议把整个项目放在WSL2的Linux文件系统中，例如：

```text
/home/你的用户名/work/claude-code-course
```

不要优先放在：

```text
/mnt/c/Users/你的用户名/...
```

你的工作流同时涉及Codex、Claude Code、Node、Python、权限隔离和大量小文件，统一放在WSL2的Linux目录更容易管理。当前Codex官方安装资料支持Windows安装，同时其系统要求文档仍把Windows 11＋WSL2列为主要开发环境；Claude Code也支持WSL和Git for Windows。([GitHub][3])

## 完整目录树

```text
claude-code-course/
│
├── AGENTS.md
├── README.md
├── .gitignore
│
├── input/
│   ├── original-course.md
│   ├── learner-profile.md
│   ├── required-topics.yaml
│   └── project-decisions.md
│
├── source-snapshot/
│   ├── SNAPSHOT.json
│   ├── source/
│   ├── tests/
│   ├── package.json
│   ├── tsconfig.json
│   └── 其他原始文件...
│
├── orchestrator/
│   ├── config/
│   │   ├── course.yaml
│   │   └── risk-rules.yaml
│   │
│   ├── prompts/
│   │   ├── codex-orchestrator.md
│   │   ├── fact-review-a.md
│   │   ├── fact-review-b.md
│   │   ├── teaching-review.md
│   │   ├── json-repair.md
│   │   └── stage-release-review.md
│   │
│   ├── schemas/
│   │   ├── fact-review-a.schema.json
│   │   ├── fact-review-b.schema.json
│   │   ├── teaching-review.schema.json
│   │   ├── adjudication.schema.json
│   │   └── knowledge-base.schema.json
│   │
│   └── scripts/
│       ├── bootstrap.sh
│       ├── build-evidence-pack.py
│       ├── invoke-fact-review.py
│       ├── invoke-teaching-review.py
│       ├── validate-review-json.py
│       ├── atomic-release.py
│       ├── propagate-revision.py
│       └── run-deepseek-claude.sh
│
├── review-template/
│   ├── CLAUDE.md
│   └── README.md
│
├── curriculum/
│   ├── design/
│   ├── global/
│   ├── units/
│   ├── stages/
│   └── redirects.json
│
├── mini-agent-harness/
│   ├── contracts/
│   ├── typescript/
│   ├── python/
│   ├── java-exercises/
│   ├── tests/
│   └── architecture/
│
└── tmp/
```

实际的DeepSeek审查运行目录建议放在项目外：

```text
/srv/claude-review-runtime/
```

这样项目内只保存审查模板和最终审计结果，不把临时依赖、构建缓存和实验副本混入课程工程。

---

# 三、哪些文件需要你现在手动放入

## 必须手动准备

### 1. 本地Claude Code CLI快照

放入：

```text
source-snapshot/
```

推荐保留原目录结构，不要为了方便分析而先手动合并文件。

例如：

```text
source-snapshot/
├── source/
│   ├── query/
│   ├── tools/
│   ├── tasks/
│   └── ...
├── tests/
├── package.json
└── tsconfig.json
```

### 2. 原始45单元课程规划

放入：

```text
input/original-course.md
```

保留你原来的单元标题、目标、实验要求和核心主题。

### 3. 学习者背景

放入：

```text
input/learner-profile.md
```

内容包括：

* Java/Spring基础；
* Python基础；
* LangChain/LangGraph经验；
* TypeScript零基础；
* 每天学习时间；
* 秋招Agent应用开发岗位目标；
* 最终需要独立构建Agent Harness。

### 4. 已经确定的全部规则

把我们前面确认的决策汇总到：

```text
input/project-decisions.md
```

此前给出的完整最终方案也可以直接放入这个文件。

### 5. 四个主要提示词

把前一条回复中的提示词分别放入：

```text
orchestrator/prompts/codex-orchestrator.md
orchestrator/prompts/fact-review-a.md
orchestrator/prompts/fact-review-b.md
orchestrator/prompts/teaching-review.md
```

其中事实审查要拆成两个文件：

```text
fact-review-a.md
只包含独立盲审规则

fact-review-b.md
只包含读取Codex结论后的对照审查规则
```

## 可以让Codex首次运行时生成

以下内容不用一开始手写：

```text
orchestrator/schemas/*.json
orchestrator/scripts/*.py
curriculum/design/*
curriculum/global/*
curriculum/units/*
mini-agent-harness/*
```

首次Codex任务可以要求它创建这些基础设施，但此时不能开始写正式教材正文。

---

# 四、推荐的`AGENTS.md`

根目录的`AGENTS.md`不要放完整数万字提示词，只保存稳定的最高优先级规则，并要求Codex读取详细提示词。

# Claude Code CLI课程工程规则

## 项目目标

本工程用于基于本地Claude Code CLI源码快照，生成30～45个系统性源码教学单元，并逐步实现TypeScript与Python版本的Mini Agent Harness。

## 启动时必须读取

开始任何任务前，必须读取：

* `orchestrator/prompts/codex-orchestrator.md`
* `input/learner-profile.md`
* `input/project-decisions.md`
* `input/original-course.md`
* `orchestrator/config/course.yaml`

这些文件共同构成本项目的完整需求。不得只依据本文件生成教材。

## 权限规则

* `source-snapshot/`是只读证据源，不得修改、格式化、重命名或生成文件。
* 教材草稿只能写入`curriculum/units/*/draft/`。
* 未通过发布流程，不得直接写入`final/`。
* DeepSeek不能直接修改教材、知识库或主项目。
* 所有DeepSeek实验必须在独立审查运行目录执行。

## 第一次运行

第一次运行只能：

1. 检查目录与输入文件；
2. 扫描本地源码快照；
3. 创建缺失的Schema、脚本和基础目录；
4. 生成全课程设计包；
5. 生成30～45单元依赖图、主题覆盖表和风险表；
6. 停止并等待用户审批。

第一次运行禁止：

* 生成正式教材正文；
* 发布任何单元；
* 自动改变用户确定的核心规则；
* 删除原45单元中的核心主题。

## 证据规则

内部实现优先依据本地源码和本地测试。

新版公开信息只能作为双版本补充，不得直接覆盖本地快照结论。

无法确认的正文结论必须紧邻标记`【推断】`。

## 工作质量

主线优先级：

1. 机制与架构理解；
2. 真实源码定位、调用链和关键实现；
3. 独立复现与修改能力；
4. 面试表达。

不得用函数名堆砌代替机制解释。

Codex会自动按目录作用域读取`AGENTS.md`，子目录如果以后需要更严格规则，还可以再放更深层的`AGENTS.md`。([GitHub][1])

---

# 五、推荐的Claude Code审查`CLAUDE.md`

这个文件放在：

```text
review-template/CLAUDE.md
```

每次创建审查工作区时，Codex将它复制到审查目录根部。

# 独立审查工作区规则

你是独立审查者，不是教材作者，也不是主项目维护者。

## 可访问范围

你只能使用当前审查工作目录中已经复制的文件。

不得主动访问：

* 原始课程工程；
* 原始源码快照目录；
* Codex会话文件；
* 其他单元；
* 用户主目录中的无关文件。

不得使用`../`或绝对路径访问当前审查目录之外的内容。

## 写入范围

你可以在当前审查目录中生成：

* 纯JSON审查结果；
* 临时实验；
* 测试代码；
* 命令日志；
* 证据清单。

不得修改或输出：

* 正式教材；
* Codex主结论；
* 全局知识库；
* 原始源码快照。

## 输出格式

最终输出只能是符合当前任务Schema的纯JSON。

不得输出：

* Markdown代码围栏；
* 前言；
* 后记；
* JSON之外的自然语言；
* 私有推理过程。

## 审查独立性

事实审查阶段A不得读取Codex详细结论。

阶段B只能在明确收到Codex结论后进行差异比较。

不得回写或修改阶段A已经保存的结果。

## 凭据规则

不得在JSON、日志、实验代码或命令输出中回显真实API Key、Token、Cookie或账号凭据。

Claude Code启动时会自动读取当前目录及父目录适用的`CLAUDE.md`，因此每个隔离审查目录都应拥有自己的副本。([Anthropic Docs][4])

---

# 六、最小`course.yaml`

放入：

```text
orchestrator/config/course.yaml
```

```yaml
course:
  name: "Claude Code CLI Source Curriculum"
  unit_min: 30
  unit_max: 45
  main_study_minutes_min: 150
  main_study_minutes_max: 180

learner:
  typescript_level: zero
  java_level: intermediate
  python_level: intermediate
  agent_frameworks:
    - LangChain
    - LangGraph

languages:
  primary_source_language: TypeScript
  clean_room:
    - TypeScript
    - Python
  java_usage: analogy_and_selected_experiments

reviews:
  fact:
    two_stage: true
    same_session: true
    max_complete_attempts: 3
    supplement_requests: 1
  teaching:
    normal_rechecks: 1
    severe_rechecks: 2
  json_format_retries: 2

release:
  mode: stage_atomic
  units_per_stage_min: 3
  units_per_stage_max: 7

uncertainty:
  inline_label: "【推断】"
```

---

# 七、`.gitignore`

不建议把泄露源码快照、API密钥、临时审查目录和依赖缓存提交到远程Git仓库。

```gitignore
# Secrets
.env
.env.*
.secrets/
*.key
*.token

# Local source snapshot
source-snapshot/*
!source-snapshot/SNAPSHOT.json

# Runtime review workspaces
tmp/
review-workspaces/
*.log

# Node
node_modules/
dist/
coverage/

# Python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Temporary drafts
*.tmp
*.bak

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
```

课程正式Markdown、审查后保留的JSON、知识库和Mini Agent Harness代码可以提交到**本地Git仓库**。是否推送到远程仓库，应等你确认源码许可与敏感内容后再决定。

---

# 八、创建目录的命令

以下按WSL2 Ubuntu执行：

```bash
mkdir -p ~/work/claude-code-course
cd ~/work/claude-code-course

mkdir -p \
  input \
  source-snapshot \
  orchestrator/config \
  orchestrator/prompts \
  orchestrator/schemas \
  orchestrator/scripts \
  review-template \
  curriculum/design \
  curriculum/global \
  curriculum/units \
  curriculum/stages \
  mini-agent-harness/contracts \
  mini-agent-harness/typescript \
  mini-agent-harness/python \
  mini-agent-harness/java-exercises \
  mini-agent-harness/tests \
  mini-agent-harness/architecture \
  tmp

touch \
  AGENTS.md \
  README.md \
  .gitignore \
  input/original-course.md \
  input/learner-profile.md \
  input/project-decisions.md \
  input/required-topics.yaml \
  source-snapshot/SNAPSHOT.json \
  orchestrator/config/course.yaml \
  orchestrator/prompts/codex-orchestrator.md \
  orchestrator/prompts/fact-review-a.md \
  orchestrator/prompts/fact-review-b.md \
  orchestrator/prompts/teaching-review.md \
  orchestrator/prompts/json-repair.md \
  orchestrator/prompts/stage-release-review.md \
  review-template/CLAUDE.md

git init
```

然后：

1. 把快照复制到`source-snapshot/`；
2. 把原课程放入`input/original-course.md`；
3. 填写学习背景；
4. 写入提示词；
5. 再初始化第一个Git提交。

```bash
git add AGENTS.md README.md .gitignore input orchestrator review-template
git commit -m "bootstrap curriculum generation workflow"
```

---

# 九、将源码快照设为真正只读

仅仅在提示词里写“不能修改”不够。需要操作系统权限。

在所有快照文件放好后执行：

```bash
sudo chown -R root:root source-snapshot

sudo find source-snapshot -type d -exec chmod 755 {} \;
sudo find source-snapshot -type f -exec chmod 444 {} \;
```

效果：

* 你的普通用户和Codex可以读取；
* 普通用户启动的Codex不能写入；
* 只有通过`sudo`才能修改快照。

需要更换快照时：

```bash
sudo chown -R "$USER":"$USER" source-snapshot
```

更新完成后再恢复只读。

---

# 十、安装和配置Codex

## 1. 安装

Codex当前官方仓库提供安装脚本，也支持npm安装；这里建议统一用npm，方便和Claude Code共用Node环境。([GitHub][3])

```bash
npm install -g @openai/codex
codex --version
```

## 2. 登录

推荐使用ChatGPT账号登录：

```bash
codex --login
```

当前官方登录流程支持使用ChatGPT身份登录Codex CLI，并把凭据保存在本地，不必手动复制OpenAI API Key。([OpenAI Help Center][5])

## 3. Codex配置

第一次不要配置复杂MCP、插件或全自动模式。

可以在：

```text
~/.codex/config.toml
```

设置：

```toml
approval_policy = "on-request"
sandbox_mode = "workspace-write"
```

Codex当前配置Schema支持`approval_policy`与`sandbox_mode`等配置。([GitHub][6])

也可以不修改全局配置，而是在启动时明确传参：

```bash
cd ~/work/claude-code-course
codex --sandbox workspace-write
```

## 4. 首次不要使用

```bash
codex --sandbox danger-full-access
```

也不要一开始就用无人监督的非交互执行。

先通过交互式Codex完成：

* 文件结构检查；
* 提示词理解；
* 设计包生成；
* 脚本和Schema建立。

工作流稳定后，再使用：

```bash
codex exec "任务"
```

`codex exec`是官方提供的非交互运行方式，可从命令参数或标准输入接收任务。([GitHub][2])

---

# 十一、安装Claude Code CLI

```bash
npm install -g @anthropic-ai/claude-code

claude --version
claude doctor
```

Anthropic官方要求Node.js 18+，并建议安装后运行`claude doctor`检查安装状态。([Anthropic Docs][7])

这里的Claude Code不需要登录Anthropic账号，因为你将通过DeepSeek的Anthropic兼容端点调用。

---

# 十二、配置DeepSeek，不要写进Markdown

DeepSeek API Key不能放入：

```text
AGENTS.md
CLAUDE.md
course.yaml
任何prompt文件
Git仓库
```

建议只在当前终端会话中设置：

```bash
read -s -p "DeepSeek API Key: " DEEPSEEK_API_KEY
echo
export DEEPSEEK_API_KEY
```

然后创建：

```text
orchestrator/scripts/run-deepseek-claude.sh
```

内容：

```bash
#!/usr/bin/env bash
set -euo pipefail

if [[ -z "${DEEPSEEK_API_KEY:-}" ]]; then
  echo "DEEPSEEK_API_KEY is not set" >&2
  exit 1
fi

export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="$DEEPSEEK_API_KEY"

export ANTHROPIC_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_OPUS_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_SONNET_MODEL="deepseek-v4-pro[1m]"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_SUBAGENT_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_EFFORT_LEVEL="max"

exec claude "$@"
```

赋予执行权限：

```bash
chmod +x orchestrator/scripts/run-deepseek-claude.sh
```

这些端点、环境变量和当前模型名来自DeepSeek官方Claude Code接入文档。([DeepSeek API Docs][8])

这样配置只影响通过该脚本启动的Claude Code，不会污染普通终端中的其他Claude Code会话。

---

# 十三、先做Claude Code＋DeepSeek冒烟测试

创建临时目录：

```bash
mkdir -p /tmp/claude-review-smoke
cp review-template/CLAUDE.md /tmp/claude-review-smoke/CLAUDE.md
cd /tmp/claude-review-smoke
```

执行：

```bash
DEEPSEEK_API_KEY="$DEEPSEEK_API_KEY" \
~/work/claude-code-course/orchestrator/scripts/run-deepseek-claude.sh \
  -p '只返回JSON对象：{"status":"ok","role":"reviewer"}' \
  --output-format json \
  --max-turns 1 \
  --permission-mode plan
```

Claude Code支持：

* `-p`非交互调用；
* `--output-format json`；
* `--max-turns`；
* `--resume`；
* `--permission-mode`；
* `--allowedTools`与`--disallowedTools`。([Anthropic Docs][9])

注意：`--output-format json`返回的是Claude Code自身的JSON包装结果。你的编排器需要从包装结果中提取模型实际返回的审查JSON，不能未经检查就把整个stdout当成`fact-review.schema.json`。

第一次冒烟测试后，保存实际输出样例到：

```text
orchestrator/schemas/examples/claude-output-wrapper.json
```

再让Codex据此编写解析器。

---

# 十四、严格隔离DeepSeek审查环境

由于你选择了“完全隔离执行”，正式运行时最好为DeepSeek创建独立Linux用户。

## 1. 创建审查用户

```bash
sudo adduser --disabled-password --gecos "" claude-reviewer
sudo mkdir -p /srv/claude-review-runtime
sudo chown -R claude-reviewer:claude-reviewer /srv/claude-review-runtime
```

## 2. 阻止审查用户读取主工程

```bash
chmod 700 ~/work/claude-code-course
```

这样：

* 你的用户和Codex可以访问主工程；
* `claude-reviewer`不能访问主工程；
* Codex必须把证据包复制到`/srv/claude-review-runtime/`；
* Claude Code只能看到复制后的内容。

## 3. 每次审查目录

例如：

```text
/srv/claude-review-runtime/
└── M12-fact-attempt-01/
    ├── CLAUDE.md
    ├── prompts/
    │   ├── stage-a.md
    │   └── stage-b.md
    ├── evidence/
    ├── source-copy/
    ├── tests-copy/
    ├── codex-claims/
    ├── experiments/
    └── results/
```

Codex完成复制后：

```bash
sudo chown -R claude-reviewer:claude-reviewer \
  /srv/claude-review-runtime/M12-fact-attempt-01
```

然后以审查用户身份运行Claude Code。

不要给Claude Code添加主项目路径：

```text
禁止使用：
--add-dir ~/work/claude-code-course
--add-dir source-snapshot
```

---

# 十五、事实审查A、B两阶段如何启动

## 阶段A

Codex先复制：

```text
事实审查A提示词
中性问题说明
源码证据包
测试
配置
CLAUDE.md
```

不复制Codex结论。

启动示例：

```bash
cd /srv/claude-review-runtime/M12-fact-attempt-01

sudo -u claude-reviewer -H env \
  DEEPSEEK_API_KEY="$DEEPSEEK_API_KEY" \
  /home/你的用户名/work/claude-code-course/orchestrator/scripts/run-deepseek-claude.sh \
  -p "$(cat prompts/stage-a.md)" \
  --output-format json \
  --max-turns 12 \
  --permission-mode plan \
  > results/stage-a-wrapper.json
```

## 阶段B

阶段A完成后，Codex再复制：

```text
Codex Claim
源码地图
证据索引
拟写教材结论
```

然后使用同一会话恢复：

```bash
claude --resume "<SESSION_ID>" \
  -p "$(cat prompts/stage-b.md)" \
  --output-format json \
  --max-turns 10
```

Claude Code官方CLI支持通过`--resume`继续指定会话。([Anthropic Docs][9])

具体`SESSION_ID`从实际Claude Code JSON包装结果中提取。不要在看见首次冒烟测试结果前硬编码字段路径。

---

# 十六、Codex第一次打开后输入什么

前提是以下文件已经存在：

```text
AGENTS.md
input/original-course.md
input/learner-profile.md
input/project-decisions.md
source-snapshot/
orchestrator/prompts/codex-orchestrator.md
orchestrator/config/course.yaml
```

进入根目录：

```bash
cd ~/work/claude-code-course
codex --sandbox workspace-write
```

然后输入下面这段：

请执行本项目的首次启动任务。

开始前：

1. 确认你已经读取根目录`AGENTS.md`。
2. 完整读取`orchestrator/prompts/codex-orchestrator.md`。
3. 读取`input/learner-profile.md`、`input/project-decisions.md`、`input/original-course.md`和`orchestrator/config/course.yaml`。
4. 检查`source-snapshot/`是否可读且不可写。
5. 不得修改、格式化或重命名`source-snapshot/`中的任何文件。

本次任务只允许完成以下内容：

1. 检查并补全课程工程目录；
2. 建立本地源码快照的全局模块清单；
3. 建立原课程核心主题覆盖清单；
4. 生成30～45单元的候选课程目录；
5. 生成单元依赖图、阶段划分和初始风险等级；
6. 生成Mini Agent Harness演进路线；
7. 生成TypeScript知识递进索引；
8. 创建后续需要的JSON Schema和编排脚本骨架；
9. 将所有课程设计产物写入`curriculum/design/`；
10. 输出一份需要我审批的课程设计总览。

本次禁止：

* 编写任何正式单元教材正文；
* 创建任何`final`教材；
* 删除原课程核心主题；
* 调用DeepSeek生成教材；
* 自动开始下一阶段。

完成课程设计包后停止，等待我审批。

---

# 十七、Codex首次运行后的预期产物

Codex第一次任务完成后，你应该看到：

```text
curriculum/design/
├── course-design.md
├── source-module-map.json
├── original-topic-coverage.json
├── unit-catalog.json
├── unit-dependencies.json
├── stage-plan.json
├── harness-roadmap.md
├── typescript-learning-index.json
├── initial-risk-register.json
└── approval-summary.md
```

此时不应该出现：

```text
curriculum/units/M01/final/M01.md
```

也不应该已经开始生成第一单元。

---

# 十八、建议的实际启动顺序

按以下顺序操作最稳：

```text
1. 安装WSL2、Node、Git、Python
2. 安装并登录Codex
3. 安装Claude Code
4. 建立课程工程根目录
5. 放入源码快照
6. 放入原课程和学习背景
7. 写入AGENTS.md与四个提示词
8. 初始化本地Git
9. 将source-snapshot设为只读
10. 配置DeepSeek启动脚本
11. 完成Claude Code＋DeepSeek冒烟测试
12. 创建独立claude-reviewer用户
13. 在课程工程根目录启动Codex
14. 输入首次课程设计任务
15. 审批课程设计包
16. 再开始第一阶段教材
```

最关键的三个原则是：

```text
Codex始终从课程工程根目录启动
DeepSeek始终从独立审查目录启动
源码快照始终依靠操作系统权限保持只读
```

[1]: https://github.com/openai/codex/blob/main/codex-rs/protocol/src/prompts/base_instructions/default.md?utm_source=chatgpt.com "codex/codex-rs/protocol/src/prompts/base_instructions/default.md at main · openai/codex · GitHub"
[2]: https://github.com/openai/codex/blob/main/codex-rs/README.md?utm_source=chatgpt.com "codex/codex-rs/README.md at main · openai/codex · GitHub"
[3]: https://github.com/openai/codex/blob/main/README.md?utm_source=chatgpt.com "codex/README.md at main · openai/codex · GitHub"
[4]: https://docs.anthropic.com/zh-CN/docs/claude-code/memory?utm_source=chatgpt.com "管理 Claude 的内存 - Anthropic"
[5]: https://help.openai.com/en/articles/11381614-api-codex-cli-and-sign-in-with-chatgpt?utm_source=chatgpt.com "Codex CLI and Sign in with ChatGPT | OpenAI Help Center"
[6]: https://github.com/openai/codex/blob/main/codex-rs/core/config.schema.json?utm_source=chatgpt.com "codex/codex-rs/core/config.schema.json at main · openai/codex · GitHub"
[7]: https://docs.anthropic.com/en/docs/claude-code/getting-started?utm_source=chatgpt.com "Set up Claude Code - Anthropic"
[8]: https://api-docs.deepseek.com/quick_start/agent_integrations/claude_code?utm_source=chatgpt.com "Integrate with Claude Code | DeepSeek API Docs"
[9]: https://docs.anthropic.com/en/docs/claude-code/cli-usage?utm_source=chatgpt.com "CLI reference - Anthropic"
