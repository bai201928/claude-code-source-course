# Codex 主控 + Claude Code/DeepSeek 双闸门教材生成系统 V3

## 1. V3 的任务

V3 是 `2.md` 的执行方案，不是另一份需求文件。它要在不恢复旧重型系统的前提下，持续生成一套源码级、适合初学者、可以实践和迁移的 Claude Code CLI 教材。

系统只优化两件事：

1. 把教材质量建立在真实运行、源码证据、可复现实验和教学校准上；
2. 删除不提升教材质量的 Hash、漂移、句子级 Claim、JSON 审计和边缘问题消耗。

系统不会把质量等同于文件数量、审查轮数或知识条目配额，也不会把 Graphify 的图关系当作事实。

## 2. 启动条件与权威输入

Codex 开始新的课程、阶段、单元或上下文恢复时，依次读取：

1. `2.md`；
2. 用户已经确认的项目决策；
3. `3.md`；
4. 经用户批准的课程设计包；
5. 与当前单元直接相关的前置摘要、知识条目和工作簿。

不把全部历史聊天、全部前置教材和全部源码塞入上下文。若批准后的设计与 `2.md` 冲突，必须先修订设计，不可继续生成。

当前源码根目录固定为：

```text
D:\agent\Claude code最新\claude-code-CLI
```

`repos/` 只在当前章节需要新版官方资料时定向读取，本轮不刷新。Graphify 缓存位于 `graphify-out/`，不进入发布物。

## 3. 系统角色

### 3.1 Codex 主控

Codex 是唯一能够修改正式产物和作出发布裁决的角色，负责：

- 课程依赖和学习闭环设计；
- Graphify 候选定位与直接源码研究；
- 运行流程、状态所有权和证据边界确认；
- 教材正文、图、代码和实验；
- TypeScript/Python Harness 契约演进；
- 调用 Claude Code/DeepSeek 并裁决问题；
- 跨单元一致性与阶段发布；
- 后续版本化纠错。

### 3.2 Claude Code/DeepSeek Max

Claude Code CLI 通过 CC SWITCH 使用 DeepSeek Max，只作为独立审查者：

- 事实闸门 A：在不知道 Codex 结论的情况下盲审源码；
- 事实闸门 B：在同一个会话中对照 Codex 的机制结论和实验；
- 教学闸门：在新的独立会话中审查初学者学习体验。

DeepSeek 不直接修改教材、正式实验、Harness、知识库、候选稿或发布目录。DeepSeek Max 不设置应用层超时，正常等待 Claude Code CLI 进程退出。

### 3.3 Graphify

Graphify 是只读导航层。它负责发现候选组件、架构中心、跨文件邻接和可能路径。它不参与闸门，不给教材提供最终证据，不决定课程是否增加章节。

## 4. 总体流水线

```text
最高需求和当前决策
        |
        v
Graphify 候选定位 -> 直接源码核验 -> 必要实验/官方资料
        |                    |
        +---------+----------+
                  v
            课程设计包
                  |
             用户审批
                  |
                  v
              标杆章
                  |
             用户确认
                  |
                  v
单元研究 -> 事实 A/B -> 教材与实验 -> 教学闸门 -> 候选稿
                  |
                  v
阶段一致性 + Harness 回归 + 主题覆盖 -> 原子发布
```

课程设计包批准前不生成正式教材正文；标杆章未被用户确认前不批量生成。

## 5. Graphify 辅助研究协议

研究跨模块问题时执行：

```text
1. 用图谱词汇查询候选节点、社区、邻居或路径
2. 标记边的 EXTRACTED / INFERRED / AMBIGUOUS 状态
3. 用 rg 定位源码并阅读决定性函数和调用者
4. 区分真实调用边、共享导入、同文件包含和图中邻近
5. 必要时运行实验或查官方资料
6. 只把核验后的结论写入工作簿
```

固定限制：

- 不自动执行 `graphify save-result`、`graphify reflect` 或 `graphify update`；
- 不安装 strict、watch、post-commit hook；
- 不从图谱关系直接生成教材段落；
- 不用图谱替代直接源码阅读；
- 不因图谱失败而中止，立即回退 `rg` 与源码。

Graphify 最短路径若经过 `imports`、`contains` 或共享符号，只能说明结构可达，不能叫作运行调用链。

## 6. 课程设计协议

课程单元数量不设最低下限，原则上不超过 45 个。本轮重新生成，不复用已删除的旧设计包。当前 42 单元设计只作为可修改的工作地图；课程设计必须从真实调用依赖、学习负荷和 Harness 演进共同推导，不为维持数量而拆章。

逐单元研究和生成过程中，Codex 可以动态合并、拆分、移动或重排尚未发布单元，并调整标题、内容边界和阶段归属。每次调整只需同步受影响的依赖、最高需求主题覆盖、TypeScript 前置和 Harness 路线，不建立重型变更系统。允许最终课程少于 30 个单元；合并必须保留完整认知闭环，不能压缩掉关键源码关系、状态/失败语义、实验或迁移能力。已经发布的单元编号原则上保持稳定。

最终课程在 M28 前完成，即使用 M01-M27、不创建 M28；剩余内容按 S3、S4、S5 发布，不再设置 S6。当章节上限要求取舍时，使用“价值优先”而不是“旧目录等比例压缩”：优先投入核心运行机制、优秀架构思想、跨组件契约、状态 owner、并发/取消/恢复、安全与可迁移实现；其次保留能解释主链的代表性变体；管理 UI、重复命令入口、枚举式选项和不改变语义的边缘分支只提供定位，不占主体篇幅。任何看似边缘的细节若会改变协议完整性、数据一致性、安全边界、恢复或 Harness 契约，自动升回核心范围。

每个单元设计记录：

- 它要解决的真实问题；
- 学习者进入前已经掌握什么；
- 本单元闭合的运行机制；
- 主要源码入口和边界；
- 主体学习预计 4 至 7 小时；
- 独立实践与深入实验，时间另计；
- 风险等级与需要验证的关键语义；
- 对 Harness 契约的影响；
- 后续依赖它的单元。

不使用固定 26 节模板，不设“每章核心事实数”“知识条目数”“面试题数”等配额。章节形态由认知闭环决定。

设计包必须包含最高需求主题覆盖矩阵，而不是不存在的旧课程文件映射。每个主题要标明主讲单元和复用单元，防止合并时遗漏。

## 7. 标杆章校准

用户批准课程设计包后，只制作一章正式质量标杆，主题固定为：

> 用户输入如何经过会话状态、Query Loop、请求投影和 Tool Loop 到达模型。

标杆章必须同时闭合两条输入路径：

```text
交互式 REPL
-> REPL 本地 messages / messagesRef（AppState 提供权限、工具等运行状态）
-> processUserInput
-> query()

SDK / Headless
-> print/SDK 共享 mutableMessages 与 QueryEngine
-> processUserInput
-> messages 快照
-> query()
```

两条路径在 `query()` 汇合，之后进入 `queryLoop`、上下文/请求投影、真实模型流和 Tool Loop。不得写成“REPL 通过 QueryEngine”。

标杆通过的标准不是结构齐全，而是学习者能沿一个真实问题自然完成：建立心智模型、走完一次运行、理解决定性源码与状态、验证行为、修改并复现、接入 Harness、迁移和表达。

用户已于 2026-07-28 确认 M11 为正式质量标杆，并增加两项强制校准：一是正文随新增信息在认知转折处配置多张局部运行图，不能只依赖章首总图；二是面试部分以资深大厂 Agent 开发面试官视角出题，每道核心题提供结论先行、口语化约两分钟回答。已确认的叙事密度、代码解释颗粒度、图文规则、实践难度和面试表达写入 `curriculum/design/benchmark-rules.md`，供后续写作与教学闸门读取。

## 8. 单元研究工作簿

每单元先建立 `unit-workbook.md`，它是作者工作区，不是教材正文。只记录有助于定稿的内容：

- 单元问题、边界和前置；
- 精简源码地图与真实调用链；
- 输入、输出、消息和关键状态；
- 状态所有者、修改者和观察者；
- 快照事实、运行验证、新版公开行为、推断、迁移设计的区分；
- 最重要的失败、取消、并发和恢复路径；
- 实验假设、反证条件和结果；
- Harness 候选契约；
- 事实闸门范围。

工作簿不拆成 Claim、证据索引、owner map、符号证据、证据胶囊、风险映射等 JSON 文件。关键结论用路径、符号、分支和辅助行号定位即可。

## 9. 风险与验证强度

风险用于决定验证力度，不用于计算文档数量。

### R1：边界清晰

调用和状态边界清楚，风险主要是跨模块理解或一般异常。

- 闭合主路径；
- 至少一个正常验证实验；
- 一轮事实 A/B；
- 一轮教学审查。

### R2：行为敏感

涉及 Context、并发、取消、权限、持久化、多 Agent、后台任务、锁或状态恢复。

- 正常实验；
- 一个最有代表性的失败、反例或竞争验证；
- 一轮事实 A/B，必要时定向复审；
- 一轮教学审查，严重问题可复审一次。

### R3：关键事实未闭合

核心调用链、状态所有权、安全或恢复语义无法确认，或源码、测试和实验发生实质冲突。

- 暂不定稿相关结论；
- Codex 定向补证；
- 最多三轮完整事实审查；
- 仍不能确认时缩小表述并紧邻标记 `【推断】`，或将该部分延后。

风险可在研究中升级。非实质格式问题、理论漏洞和不会影响教材质量的边缘问题不升级风险、不阻断。

## 10. 事实闸门 A：独立盲审

Codex 为当前单元准备中性范围说明 `fact-gate-scope.md`，包含：

- 当前机制和要回答的问题；
- 源码根目录；
- 建议优先阅读的路径与符号；
- 必要测试、类型、fixture 和配置；
- 快照与官方资料的边界；
- 禁止修改正式目录。

阶段 A 不提供：

- Codex 的机制结论；
- Codex 的完整调用图；
- 拟写入教材的叙事；
- Graphify 查询或图谱关系；
- 教学别名暗示的职责。

DeepSeek 独立确认入口、主调用链、消息和状态变化、所有权、生命周期、失败/取消/恢复，以及无法确认项。它只报告会影响结论的问题，不总结整个仓库。

输出头部固定为：

```text
GATE: FACT_A
VERDICT: PASS | REVISE | BLOCK
MATERIAL_ISSUES: <number>
```

## 11. 事实闸门 B：同会话对照

阶段 A 完成后保留同一个 Claude Code 会话 ID，新增：

- Codex 的机制摘要；
- 精简真实调用链；
- 状态所有权结论；
- 拟写入教材的关键表述；
- 实验设计或已经得到的实验结果。

不重复发送源码，不发送 Graphify 输出。DeepSeek 只检查错误、重要遗漏、证据不足、版本混淆和推断冒充事实。

输出头部固定为：

```text
GATE: FACT_B
VERDICT: PASS | REVISE | BLOCK
MATERIAL_ISSUES: <number>
```

第一次完整事实审查最多接受一次有明确路径、符号、理由和验证目标的补证请求。禁止连续零碎索取和全项目扩散。

## 12. Codex 裁决

事实闸门意见不能自动进入教材。Codex 对每个实质问题记录：

```text
Issue: <稳定 ID>
Decision: accepted | rebutted
Reason: <源码、测试或实验理由>
Change: <实际修改或边界说明>
```

若 accepted 问题影响正文、图、实验、Harness 契约或面试表达，Codex 直接同步修改这些产物，不建立额外影响传播图。若问题无关事实正确性、初学者理解、实验有效性和 Harness 契约，记录为非阻断并结束，不消耗额外轮次。

## 13. 教材写作协议

教材按认知过程自然推进：

```text
真实问题
-> 心智模型
-> 一次运行
-> 调用链与决定性源码
-> 状态与组件协作
-> 失败、取消和恢复
-> 验证实验
-> 修改与复现
-> Harness 集成
-> 企业迁移与面试表达
```

这是一条作者检查线，不是固定标题模板。写作时：

- 先让学习者知道正在解决什么，再引入符号；
- 解释代码为何改变控制流和语义，不做函数逐项说明；
- 章首总图先建立全局定位；正文后续出现多层投影、状态所有权、重要分支、反馈循环、取消恢复或跨组件时序时，必须在就近位置用局部流程图、时序图、状态图或所有权图再次收敛理解；
- 每张图只回答一个明确问题，同时服务首次理解和后续复习；图中箭头、所有权、分支和文本必须与源码、实验一致，不设固定数量，不添加装饰性图示；
- 表格只用于精确映射、对比或状态归属；
- TypeScript 难点在首次需要时讲透，并给 Java/Python 对照；
- 实验嵌入推理过程，不放成与正文无关的附录；
- 企业迁移和面试表达从当前机制自然推出；面试题以资深大厂 Agent 开发面试官视角选取，核心题给出“先结论、再详细机制”的口语化约两分钟回答，题量由机制决定而非配额；
- 证据定位放在关键结论附近或精简附录，不把正文写成审计报告。

## 14. 代码、实验与 Harness

每单元实现：

- TypeScript clean-room 核心机制；
- Python 同行为契约复现；
- 必要的 Java/Spring 可运行练习；
- 一个主验证实验；
- 按风险选择的失败、反例、取消、恢复或竞争实验。

实验必须记录预期、观察点、实际结果和结论，不只保存“passed”。

完成独立实验后，Codex 对 Harness 作：

- `merge`：契约稳定、测试通过，适合进入累计主线；
- `defer`：机制正确但前置、抽象或兼容性尚未成熟；
- `reject`：仅适合教学实验，合入会扭曲 Harness 架构。

每次决定记录影响模块、行为契约、状态所有者、兼容风险和回归结果。不使用接口 Hash 或 owner Hash。

## 15. 教学闸门

教学闸门必须使用新的 Claude Code/DeepSeek 会话，与事实 A/B 隔离。它只读取：

- `2.md` 中的学习者与教学标准；
- 用户确认后的标杆规则；
- 当前 `draft.md`；
- 当前单元必要的前置摘要；
- 本单元学习目标和主体 4 至 7 小时边界。

默认不读完整源码、Graphify、事实审查、实验日志和全局知识库，避免被作者结论锚定。

重点检查：

- 是否只是函数名或知识点堆砌；
- 是否跳过关键 TypeScript/Node 前置；
- 是否讲清真实问题、运行过程和设计动机；
- 消息、状态、所有权、失败和恢复是否可跟踪；
- 图、代码、叙述和实验是否一致；
- 图是否在后续信息增量与认知转折处就地出现，而不是只有章首总图；局部图是否既能辅助当前理解，又能独立用于复习；
- 初学者能否独立运行、观察、修改和复现；
- Java/Python/LangGraph 对照是否准确且有帮助；
- Harness 接入和企业迁移是否由当前机制推出；
- 面试问题是否像资深 Agent 开发面试官的真实追问，回答是否结论先行、口语化且能在约两分钟内展开到 Claude Code 机制和工程边界；
- 正文是否在合理密度下完成 4 至 7 小时主体学习。

输出头部固定为：

```text
GATE: TEACHING
VERDICT: PASS | REVISE | BLOCK
MATERIAL_ISSUES: <number>
```

普通问题只审查一次，Codex 修改后自行检查；严重认知跳跃、图文冲突、核心代码无法理解等问题最多复审一次。措辞偏好和无学习影响的问题不形成 Issue。

## 16. 单元目录与状态

单元使用轻量目录：

```text
curriculum/units/M11/
|- unit-workbook.md
|- fact-gate-scope.md
|- fact-review.md
|- draft.md
|- teaching-review.md
|- implementation-report.md
|- release-candidate.md
|- final.md                 # 仅阶段原子发布后存在
|- code/
|  |- typescript/
|  |- python/
|  `- java-exercises/       # 仅必要单元
`- experiments/
```

状态流转为：

```text
planned
-> researched
-> fact-reviewed
-> drafted
-> implemented
-> teaching-reviewed
-> release-candidate
-> final
```

不影响教材质量的清理问题可以在阶段完成时统一改变状态，不要求中途反复修复。`final.md` 不得在单元单独通过时提前生成。

命令输出、调试日志、临时缓存、草稿碎片和实验过程文件可以不跟踪、不审查、不整理。它们不需要进入上述目录结构，也不要求单元或阶段结束时清理。只有过程性写入影响源码快照、正式产物、实验可信度、Harness 契约、凭据安全或原子发布时，才升级为需要处理的问题。

## 17. 单元执行流程

```text
1. 重读 2.md、3.md、批准设计和必要前置
2. 用 Graphify 定位候选关系
3. 直接阅读源码并闭合真实运行链
4. 创建 unit-workbook.md 与风险判断
5. 设计验证实验和 Harness 候选契约
6. 运行事实闸门 A
7. 同会话运行事实闸门 B
8. Codex 裁决并修正研究结论
9. 编写连续叙事的 draft.md
10. 完成 TypeScript/Python 实现与实验
11. 按风险完成代表性失败或恢复验证
12. 运行独立教学闸门
13. Codex 修改并做图文/代码/实验自检
14. 作出 Harness merge/defer/reject 决定并回归
15. 生成 release-candidate.md
16. 等待本阶段其他单元，不提前发布 final
```

除非用户要求停下，Codex 可以连续执行设计已批准的多个单元；但每单元仍要完成完整状态流转，不能用批量写作跳过研究和闸门。

## 18. 阶段检查与原子发布

阶段内所有单元达到 `release-candidate` 后，Codex 检查：

- 组件职责、状态所有权和调用链是否冲突；
- 快照事实、运行验证、新版行为、推断和迁移设计是否混淆；
- 术语、教学别名、真实符号和图是否一致；
- 单元依赖是否正确，是否有认知跳跃或大面积重复；
- 最高需求主题是否遗漏；
- TypeScript 难点是否在使用前讲解；
- TypeScript/Python 行为契约是否一致；
- Harness 新增能力与全量回归是否通过；
- 实验是否真正支持正文结论。

只有发现冲突时才定向回源码，不重做全阶段事实审查，不扫描全部源码，不生成 Hash 或证据图。

检查通过后，把本阶段所有候选同时发布为 `final.md`，并更新全局术语、TypeScript 索引、源码符号映射、知识摘要和 Harness 版本。发布过程失败时保留之前的正式状态。

## 19. 知识与版本化纠错

全局知识仅保存后续课程真正需要复用的已确认机制：术语、教学别名、真实符号、职责、状态所有权、关键调用关系、来源单元、证据状态、替代关系和使用位置。

不保存普通教学解释、局部变量、每个函数、每个失败案例、实验日志、Hash 或句子引用。

后续证据推翻旧结论时：

1. 写明新结论和替代关系；
2. 定向检查受影响的正文、图、代码、实验、Harness 和面试表达；
3. 修订受影响单元并记录简短变更；
4. 不进行全课程自动传播扫描。

## 20. 失败与回退

- Graphify 不可用：回退 `rg` 和源码阅读，继续研究。
- 官方仓库不匹配：只保留快照结论，标记新版行为待查。
- 实验环境暂不可运行：不得伪造结果；缩小结论或标为待验证。
- Claude Code/DeepSeek 进程运行很久：继续等待，不设置应用层超时。
- DeepSeek 调用失败：保留当前状态和会话信息，重试闸门；不得自行标记通过。
- 闸门只报无实质影响问题：Codex 记录非阻断并继续。
- R3 三轮仍不确定：缩小范围、标记 `【推断】` 或延后，不无限补证。
- MCP 启动失败：不影响 Graphify CLI 和直接源码研究，也不阻断课程。

## 21. 明确禁止的旧机制

V3 不执行：

- 源码、片段、Claim、owner、接口、段落和审查 Hash；
- 漂移验证或重哈希；
- 句子级 Claim；
- JSON 审查、JSON Schema 和 JSON 修复重试；
- 全量证据图、owner 图、段落依赖图；
- 每个异常分支或错误码的实验；
- 每轮审查建立新目录；
- 事实 A/B 重复复制源码；
- 教学闸门读取事实审查以形成锚定；
- Graphify 回写、自动更新或直接充当证据；
- 无实质影响的漏洞或清理问题阻断；
- 盘点、格式化、归档或清理不影响正式结果的中间输出与过程性写入；
- 自动提交 Git。

## 22. 当前实施状态

已完成：

- `input/graphify-audit.md`；
- 修订后的 `2.md`；
- 本 `3.md`；
- 经用户批准并按最新决策重排的全课程设计包；最终使用 M01-M27，剩余内容发布为 S3、S4、S5，不设置 S6 或 M28；
- M11 事实闸门 A/B、教材正文、TypeScript/Python clean-room 实验、教学闸门与 Harness `merge` 裁决；
- `curriculum/units/M11/release-candidate.md`。
- 用户对 M11 正式标杆的批准，以及 `curriculum/design/benchmark-rules.md`。
- M10-M15 的源码研究、事实 A/B、Codex 裁决、双语言实验、教学闸门、76/76 Mermaid 渲染与阶段一致性检查；六个单元已随 S2 原子发布为 `final.md`；
- M01-M09 核心机制串联复习章 I01：19 张已渲染图、7 组实验、5 个修改挑战、8 道两分钟面试题，事实 A/B 与教学闸门均为 `PASS / 0`，已经发布为 `final.md`；
- M12 Query 控制单元：事实 A/B、TypeScript `6/6`、Python `5/5`、11 张局部图、教学闸门 `PASS / 0`、Harness `merge + defer` 裁决与 `release-candidate.md`；
- M13 请求投影单元：事实 A/B 修订与同会话复审 `PASS / 0`、TypeScript `9/9`、Python `8/8`、17 张局部图、教学闸门 `PASS / 0`、Harness `merge + defer + reject` 裁决与 `release-candidate.md`；
- M14 真实模型流单元：事实 A `REVISE / 3` 后同会话事实 B `PASS / 0`、TypeScript `4/4`、Python `3/3`、13 张局部图、教学闸门 `PASS / 0`，并把固定容量、单消费者、close-to-abort/cleanup 的 provider-neutral stream 合入 Harness；
- S2 时的 Mini Agent Harness `0.3.0 / H2` 基线：TypeScript 主实现、Python 行为镜像、revisioned ConversationStore、请求/能力快照、strict projection、真实 Provider 端口、有界单消费者流、safe batch/exclusive barrier、固定并发上限、顺序提交、Permission-aware Tool Loop、取消配对、受限工作区工具和 metadata-only Trace；
- S3/M16-M19：Context budget、Compact transaction、Instruction Pipeline、Session/Auto Memory 与跨会话召回已经完成事实/教学双闸门、60/60 Mermaid 渲染和原子发布；
- Mini Agent Harness `0.4.0 / H3`：新增 aggregate replacement ledger、Compact prepare/commit/recovery、scoped/trusted InstructionCatalog 与 candidate-gated MemoryStore/MemoryProjector；TypeScript `69/69`、Python integrated `43/43`、strict typecheck 与累计回归通过；
- S4/M20-M23：Tool/Permission/Hook、Skill/Plugin、MCP session、Runtime Task/Work-item/Subagent/Team/Mailbox/Cron 已完成事实/教学双闸门、73/73 Mermaid 渲染和原子发布；
- S4 时的 Mini Agent Harness `0.5.0 / H5`：新增 ExtensionDecisionPipeline、ExtensionRegistry、McpSession、WorkItemStore、RuntimeExecutionRegistry、TeamDirectory、AcknowledgedMailbox、ShutdownCoordinator，以及 H6 foundation 的 DurableScheduler；当时 TypeScript `105/105`、Python integrated `79/79`、strict typecheck 与累计回归通过；
- S5/M24-M27：Transcript/Resume、Sandbox/security、observability/cost/governance 与 production release 已完成事实/教学双闸门、`78/78` Mermaid 渲染和原子发布；
- Mini Agent Harness `0.7.0 / H7`：新增 TranscriptStore、RecoveryReducer、ResumeCoordinator、PolicyEngine、SecurityExecutor、metadata-only telemetry、usage/cost/evaluation ledgers、TenantGovernor 和 ReleaseController；TypeScript `144/144`、Python integrated `116/116`、strict typecheck、累计回归、两个 demo 与 Compose 静态配置通过；

S0 的 M01-M04 与 H0、S1 的 M05-M09 与 H1、S2 的 M10-M15 与 H2、S3 的 M16-M19 与 H3、S4 的 M20-M23 与 H4/H5、S5 的 M24-M27 与 H6/H7 均已原子发布。最终 Harness 基线为 TypeScript `144/144`、Python integrated `116/116`、Python ConversationStore `13/13`、strict typecheck PASS、H2 `4/4`、H1 `12/12`、S0 `15/15`、集成回归 `4/4`。Claude Code CLI 教材在 M27 完成，不创建 M28 或 S6；后续 Harness 演进不再自动扩展课程章节。

最终原则：

> Codex 负责把证据变成理解，把理解变成可运行能力；双闸门负责发现重大错误，Graphify 负责缩短寻找路径。三者都不能取代学习者最终形成的完整心智模型。
