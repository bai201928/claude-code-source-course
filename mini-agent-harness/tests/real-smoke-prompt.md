# Real-provider smoke prompt

Use the `list_files` tool to inspect at most five Markdown files in the current workspace. Then answer with one short sentence that states how many paths the tool returned. Do not run commands and do not modify files.

