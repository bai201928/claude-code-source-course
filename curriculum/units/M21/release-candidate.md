# M21 Release Candidate

正式候选正文为 `draft.md`。

状态：release-candidate，等待 S4/M20-M23 原子发布。

验收摘要：

- FACT_B PASS / 0；FACT_A 的五项计数均为已成功反证的常见误写；
- TEACHING PASS / 0；
- 14/14 Mermaid 实际渲染；
- TypeScript/Python H4-2 各 8/8，strict typecheck 通过；
- 不创建 `final.md`，不单独提交或 push。
