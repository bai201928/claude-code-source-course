# M11 教学闸门记录

状态：`teaching-reviewed`

审查会话：`41fb7008-f7aa-4b12-b3fa-fe7aef370eb0`

审查模型：Claude Code CLI 经 CC SWITCH 调用 DeepSeek Max，主审实际模型记录为 `deepseek-v4-pro[1m]`。进程自然退出，无应用层超时，无权限拒绝。

## 闸门结果

```text
GATE: TEACHING
VERDICT: PASS
MATERIAL_ISSUES: 0
```

原始输出没有把规定的三行头放在首行，但在总体判断与最终评估中明确给出“达标”、“可进入下一单元”和“无阻断问题”，且所有具体建议都标为非阻断。上述三行是对审查者明确结论的规范化记录，不是 Codex 自行改变判定。

审查者在教学评估中额外查看了学习者要运行的 clean-room 代码，超出了提示词中“仅使用代码运行上下文”的限制。它没有读取事实审查、Graphify 或源码快照，额外内容只用于检查学习者实际使用的练习代码与正文是否一致，未引入事实闸门锚定。这是一个不影响教学结论的范围扩展，不因此重跑审查。

## 通过理由

审查者确认：

- REPL 与 SDK/Headless 两条路径的差异、`query()` 汇合点和“REPL 不经 QueryEngine”讲解清晰；
- 会话历史、Query Loop 状态、请求视图与 API 参数四层心智模型是全文有效主线；
- Tool Loop 以两次模型请求和中间 `tool_result` 协议闭环呈现，而不是黑盒函数回调；
- TypeScript/React 概念在它们改变当前语义时引入，并使用 Java/Python 对照补偿学习者前置；
- 双语言契约测试、可证伪的破坏实验和时间线能支持独立观察与修改；
- Harness 差异边界诚实，Java/Spring、RAG、LangGraph、企业治理和面试表达均由当前机制自然推出。

## Codex 裁决

### T-N01 `structuredClone` 说明不足

Decision: `accepted`

Reason: 这不是阻断问题，但正文刚刚使用浅复制建立源码心智模型，如果不说明实验为何改用深复制，初学者容易把两种语义混同。

Change: 已在 Harness 段落增加 `structuredClone()` 的作用、能力边界、与数组展开的区别，并强调全量深复制只是本实验为简化隔离证明作出的设计迁移。

### T-N02 Python 取消示例的 token 引用冗余

Decision: `accepted`

Reason: 审查者将实例字段与 `execute()` 参数误解为两个 token；它们实际指向同一对象，但该冗余形状确实会增加初学者负担。

Change: `CancellingTool.execute()` 现在直接取消参数传入的 `CancellationToken`，删除不必要的构造器字段，行为不变。

### T-N03 时间提示、React 可跳过说明与行号提醒位置

Decision: `rebutted`

Reason: 这些均被审查者明确标为非阻断建议。正文已用 Java 对照和当前不变量讲清 React ref，要求“跳过机制细节”会削弱标杆章的源码深度。行号已被标为辅助且末尾任务要求按符号搜索，不影响定位能力。

Change: 无。

## 复审判断

未发现严重认知跳跃、图文冲突或核心代码不可理解问题。根据 `3.md` 的教学闸门规则，普通问题修改后由 Codex 自检，不消耗第二轮模型复审。

结论：M11 已通过教学闸门，可进入候选稿自检。

## 用户批准后的标杆增补

2026-07-28，用户在审阅候选内容后明确批准 M11 标杆，并确认内容与审查质量满足预期。用户同时要求增加两项课程级标准：在后续认知转折处就近提供多张局部运行图，以及以资深大厂 Agent 开发面试官视角提供结论先行、口语化约两分钟的核心题回答。

Codex 已按此要求修订正文：Mermaid 图由 2 张增至 9 张，面试部分由单一表达升级为 7 道机制驱动题。新增图全部实际渲染通过，双语言测试与 demo 全部回归通过。此次增补没有改变已经通过 FACT_A/FACT_B 的调用链结论、实验行为或 Harness 契约；用户对修改后内容的直接批准作为最终标杆校准，不再为非实质变更重复消耗 DeepSeek 教学闸门。
