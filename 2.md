# Claude Code CLI 源码教材生成系统最高需求

## 1. 文件地位与权威顺序

本文是项目最高需求文件，约束课程设计、源码研究、教材正文、实验、Mini Agent Harness、审查与发布。任何教材任务、阶段、单元以及上下文恢复后，Codex 都必须先完整读取本文和 `3.md`。

发生冲突时按以下顺序裁决：

```text
2.md
-> 用户确认的项目决策
-> 3.md
-> 批准后的课程设计包
-> 当前章节研究工作簿
```

Graphify、审查模型、旧设计包、脚本默认值和历史文件都不能覆盖本文。自然语言提示词应保存为 `.md` 文件，不把完整规则反复塞入首次聊天消息。

## 2. 学习者与最终目的

学习者正在准备 2026 年中国互联网大厂秋招，目标包括 Agent 应用开发、LLM 应用开发、Java 后端与 Agent 结合、RAG 与企业级智能体岗位。

已有基础：

- Java、Spring 和 Python；
- LangChain、LangGraph、Agent 与 RAG 应用开发经验；
- 能编写应用，但 TypeScript 接近零基础；
- 尚未系统掌握 Node.js 运行模型和成熟 Agent Harness 的内部机制。

教材不是 Claude Code 功能说明书，也不是源码函数目录。它要以真实 Claude Code CLI 源码为观察对象，让学习者逐步具备以下能力：

```text
看懂源码
-> 理解运行机制
-> 解释设计动机
-> 跟踪调用、消息与状态
-> 验证行为
-> 修改机制
-> 复现机制
-> 集成自己的 Agent Harness
-> 迁移到企业项目
-> 完成面试表达
```

教材完成后，学习者应能独立解释和画出主要运行过程，定位入口、核心函数和决定性代码，判断状态归属与修改边界，设计验证实验，分析并发、取消、异常和恢复，扩展对应机制，并将其迁移到 Java/Spring、RAG 或其他 Agent 系统。

## 3. 教材应有的教学深度

每个机制必须在连续叙事中回答这些问题：

- 为什么会出现这个真实问题，简单实现为何不够；
- 该机制位于 Agent 系统的什么位置，前后依赖是什么；
- 一次真实运行如何触发它，控制流、消息、数据和状态如何变化；
- 哪个组件拥有状态，谁能修改，谁只能观察；
- 正常路径、取消、异常、并发竞争和恢复如何处理；
- 决定性源码为何改变了运行语义，而不只是“某函数做什么”；
- 当前设计解决了什么约束，替代设计的收益和代价是什么；
- 如何用实验推翻或确认自己的理解；
- 如何在 TypeScript 与 Python 中 clean-room 复现；
- 如何把行为契约接入累计演进的 Mini Agent Harness；
- 如何迁移到企业级 Agent、RAG、Java/Spring 和分布式系统；
- 面试中如何先给结论，再承接源码追问和系统设计追问。

这些不是固定栏目，也不得机械地变成章节标题。作者应把它们作为隐形覆盖标准，随认知过程自然出现。教材正文应像一次由问题推动的源码探索，而不是函数、表格、流程图、实验和面试题的拼装。

图必须跟随理解负荷分布，不能只在章节开头放一张总图，然后让学习者靠记忆跟完后续所有细节。当正文新增了多层投影、状态所有权、重要分支、反馈循环、取消恢复或跨组件时序时，应在合适位置增加局部运行图、时序图、状态图或所有权图，既帮助首次理解，也作为复习索引。图的数量不设配额，一张图只解决一个明确问题；节点、箭头、状态归属和正常/失败路径必须与已核验源码和实验一致，不用装饰性图示充数。

## 4. 学习时间与课程规模

- 课程单元数量不设最低下限，原则上不超过 45 个；数量由真实调用依赖和学习闭环决定，而不是把 30 个或当前设计包中的 42 个当作交付指标。
- 每个单元的主体是 4 至 7 小时的完整深度学习。
- 动手实践、失败注入、扩展挑战和深入实验另计，不得通过删减主讲解把主体强行压短。
- 每阶段约 3 至 7 个单元；阶段边界应对应可运行能力和 Harness 里程碑。
- 一个单元过大到无法形成单一认知闭环时必须拆分；多个单元只是重复同一运行过程时可以合并。

课程设计包中的单元和阶段是可演进的工作地图，不是冻结合同。随着逐章源码研究、实验和实际成章，Codex 可以合并、拆分、移动或重排尚未发布的单元，调整标题、边界、内容和阶段归属；必要时课程总量可以低于 30。动态调整必须同时更新依赖、最高需求主题覆盖、TypeScript 前置和 Harness 演进，不能以减少章节为理由删去重要机制、失败语义、实践闭环或企业迁移。已经发布的单元编号原则上保持稳定，除非用户明确批准重排。

在 M28 前完成教材，即最终使用 M01-M27、不创建 M28；剩余内容重排为 S3、S4、S5，不再设置 S6。这个边界意味着必须按价值压缩，而不是平均压缩。剩余课程优先保留和讲深：决定真实运行语义的核心机制、状态所有权、跨组件协作、并发/取消/失败/恢复、可用实验验证的行为契约、可进入 Harness 的架构能力，以及能迁移到企业 Agent/Java/Spring/RAG 的优秀设计思想。只影响管理 UI、命令外观、枚举式配置、重复适配路径或不改变上述语义的边缘细节，可以缩为定位索引或合入相邻叙事；但一旦它影响安全、协议、恢复、数据一致性或架构权衡，就必须恢复为主体内容。

“TypeScript 与 Node 前置”不是只覆盖一个最小固定点。它应提供阅读主线源码所需的最小但完整基础，并在后续首次遇到生成器、`AsyncIterable`、Stream、`AbortSignal`、事件循环、闭包、类型守卫和高阶函数时继续随用随讲。课程设计必须维护 TypeScript 主题覆盖，防止“前置精简”演变成重要难点遗漏。

## 5. 课程组织原则

课程沿一次 Agent 运行的真实依赖组织，而不是沿源码文件夹组织。总体主题包括：

1. TypeScript 与 Node.js 源码阅读基础；
2. CLI 入口、运行表面、配置、信任和初始化；
3. 用户输入、会话消息、Query、模型请求和 Tool Loop；
4. Context Pipeline、压缩、指令与记忆；
5. Tool、Permission、Hook、Skill、MCP 和 Plugin 的扩展协作；
6. Runtime Task、Work-item Task、Subagent 和 Agent Team；
7. Transcript、恢复、后台任务和 Cron；
8. Sandbox、安全、可观测性、成本与企业治理；
9. 累计演进并最终可部署的 Mini Agent Harness。

目录必须根据真实源码关系拆合。Context 不能被当成单一组件；Permission 与 Sandbox 不能视为同义；Runtime Task 与 Work-item Task 必须区分；Agent Team 不能描述成“多个 Subagent”；Transcript 写入、链重建、状态恢复和后台调度不能混成一个笼统章节。

课程正文开始前必须先交付全课程设计包，并由用户审批。设计包至少包含：

- 全课程源码机制地图；
- 单元目录、学习问题、边界与阶段划分；
- 单元依赖图；
- 最高需求主题覆盖矩阵；
- TypeScript 难点出现位置；
- Mini Agent Harness 契约演进路线；
- 标杆章范围与质量验收规则；
- 已知风险、推断和待验证项。

旧课程文件已经删除，不能假定旧设计包仍可复用，也不再维护无法执行的新旧课程文件映射。用最高需求主题覆盖矩阵证明核心主题没有遗漏。

## 6. 源码级理解标准

每个单元必须围绕真实运行闭合源码关系，而不是只列相关文件。源码研究至少要确认：

- 外部入口与运行模式；
- 组件调用边界和关键依赖注入点；
- 输入对象、输出对象和中间投影；
- 关键状态的创建、读取、修改和持久化位置；
- 主路径及最能改变语义的分支；
- 异步迭代、流、事件、锁、取消和清理；
- 错误如何进入消息、状态、日志或恢复流程；
- 交互式、SDK、Headless、Subagent 等路径是否共享同一入口；
- 同名概念是否代表不同机制。

如果源码被压缩、混淆或符号不稳定，教材可使用教学别名，但必须同时保留真实文件与符号定位。教学别名不能伪装成真实函数名。

核心源码定位采用：

```text
文件路径 -> 源码符号 -> 决定性分支或调用 -> 辅助行号
```

行号只是辅助。禁止大段复制专有源码；正文只展示理解机制所需的决定性片段，其余通过路径、符号、测试和可运行实验定位。

## 7. 证据、版本与推断

内部实现问题的证据优先级：

1. 当前本地 `claude-code-CLI/` 源码快照；
2. 快照内测试、类型、fixture、mock 和配置；
3. 与主题匹配的官方仓库源码；
4. 官方文档与 CHANGELOG；
5. 高质量第三方材料，仅作补充。

公开行为、当前配置和新版能力优先依据官方资料。新版公开行为不能改写旧快照中的内部事实，快照实现也不能冒充当前产品承诺。

教材必须清楚区分：

- `快照事实`：由当前源码或测试确认；
- `运行验证`：由可复现实验确认；
- `新版公开行为`：由官方资料确认；
- `推断`：证据不足但为解释所需，必须紧邻标记 `【推断】`；
- `设计迁移`：作者提出的 clean-room 或企业方案，不得伪装成 Claude Code 实现。

不建立句子级 Claim、证据图、Hash、漂移矩阵或段落绑定。只在作者工作簿中记录影响机制理解的结论、源码位置、证据状态与争议。

## 8. Graphify 的边界

Graphify 是源码导航辅助层，只能用于：

- 发现候选架构中心；
- 定位跨目录组件；
- 提示可能的调用路径、邻居和社区；
- 帮助决定下一步阅读哪些源码。

固定流程为：

```text
Graphify 定位候选关系
-> Codex 阅读真实源码
-> 实验或官方资料交叉验证
-> 双闸门审查
-> 才能写入教材
```

`EXTRACTED` 关系仍需核对源码；`INFERRED` 和 `AMBIGUOUS` 只能作为待验证假说。共享导入、同文件包含和图中最短路径不得自动解释为真实运行调用链。

Graphify 输出不能作为教材、图示、实验结论或面试答案的最终证据。不得自动运行 `save-result`、`reflect`、`update`，不得把模型回答写回图谱形成循环证据。Graphify 失败时直接回退到 `rg` 和源码阅读，不阻断教材研究。

## 9. TypeScript、Python、Java 与 LangGraph 教学

正文以 TypeScript 源码和 TypeScript clean-room 实现为主。对学习者尚未掌握的语言机制，首次使用时必须讲清：

- 最小语义模型；
- 在当前源码中的具体作用；
- 它如何改变异步、状态或类型边界；
- Java 与 Python 中最接近但不完全相同的概念；
- 常见误解及一个可运行的小实验。

核心机制同时提供 Python clean-room 复现。TypeScript 与 Python 不逐行翻译，而是遵守同一行为契约：输入、输出、消息、状态转换、取消、异常与测试一致；实现方式符合各自生态。

Java 主要承担接口、状态机、并发、持久化、Spring 组件边界和企业迁移对照。任务调度、事件总线、持久化、取消等少数关键机制应提供 Java 可运行练习。LangGraph 对照用于解释框架编排与 Harness 控制面的边界，不能代替源码分析。

全课程维护 TypeScript 知识索引，记录某个语言难点在哪个单元首次讲解、何处复用、是否仍有未覆盖前置。

## 10. 实验与修改能力

每单元至少有一个能够验证核心机制的独立实验。实验不是展示代码能跑，而是要明确：

- 被验证的结论；
- 可观察输入、输出和状态；
- 正常路径；
- 对该机制最有代表性的边界、失败或竞争条件；
- 预期现象与反证条件；
- 实际结果和结论边界。

实验强度按风险决定：一般机制验证主路径，高风险机制增加代表性失败或反例，涉及并发、恢复、安全和持久化时增加竞争、崩溃或恢复验证。禁止为每个 `catch`、错误码或次要分支穷举实验。

学习任务应自然包含概念复述、源码定位、流程补全、代码修改、故障诊断、系统迁移和面试表达，但不要求每次以相同栏目出现。参考答案可以折叠，不应打断主叙事。

## 11. Mini Agent Harness

课程必须维护累计演进的 TypeScript 与 Python Mini Agent Harness，而不是最后突然给出成品。Harness 是对行为和设计思想的 clean-room 迁移，不复制 Claude Code 私有实现。

其契约应逐步演进：

```text
消息与运行契约
-> Query Loop 与模型适配器
-> 流式与 Tool Loop
-> Context 投影与压缩
-> Tool/Permission/Hook/Skill/MCP/Plugin 扩展边界
-> Runtime Task 与 Work-item Task
-> Subagent 与 Agent Team
-> Transcript、恢复、后台任务与调度
-> Sandbox、可观测性和企业治理
```

每次接入必须说明新增契约、状态所有者、失败语义、兼容影响和回归结果。Codex 对实验成果作 `merge`、`defer` 或 `reject` 判断；不为了进度强行合并，也不以接口 Hash 等审计产物代替测试。

## 12. 企业迁移与面试表达

企业级思考应在每个相关机制附近即时出现，并在后期形成完整专题。至少覆盖：

- 权限、Sandbox、安全边界与供应链；
- 可观测性、评估、日志、指标与追踪；
- Transcript、幂等、恢复和数据治理；
- Token、上下文、延迟和成本治理；
- 后台任务、并发与分布式执行；
- 多 Agent 身份、任务所有权和通信治理；
- 灰度、回滚、SLI/SLO 和生产部署。

面试表达也应从机制讲解中自然生长。作者要以互联网大厂 Agent 开发岗位的资深面试官视角，提出会直接涉及或可能延伸到当前机制的问题。每道核心题都给出面试现场能够组织出来的口语化约两分钟回答：先给结论，再展开详细机制，并根据问题自然连接 Claude Code 的决定性设计、源码定位、失败边界、权衡或企业迁移。题目数量由本单元的机制广度和面试价值决定，不设配额，不用短句题库或背诵稿堆积替代教学。

## 13. Codex 与双闸门

Codex 是唯一主控，负责源码研究、课程设计、教材写作、代码与实验、审查编排、意见裁决、Harness 架构和阶段发布。

Claude Code CLI 已通过 CC SWITCH 接入 DeepSeek。DeepSeek Max 只承担独立审查，不直接修改教材、正式代码、知识库或发布目录。

每单元使用两类闸门：

- 事实闸门：独立源码盲审后，在同一 Claude Code 会话中对照 Codex 结论；
- 教学闸门：使用新的独立会话，只按学习者背景、本文要求和已确认标杆审查正文。

审查只报告影响事实正确性、初学者理解、实验有效性或 Harness 契约的问题。普通措辞、格式偏好、无现实影响的漏洞和边缘问题不得阻断。Codex 对每个实质问题作接受或反驳裁决。

DeepSeek Max 调用不设置应用层超时，等待 Claude Code CLI 自然退出。Graphify 不进入事实闸门，也不向审查者提供图谱结论作为证据。

## 14. 生成、校准与发布

执行顺序固定为：

```text
最高需求与项目决策
-> Graphify 辅助审计和直接源码研究
-> 全课程设计包
-> 用户审批
-> 一章正式质量标杆
-> 用户确认标杆
-> 按单元研究、生成和审查
-> 阶段一致性与 Harness 回归
-> 阶段原子发布
```

标杆主题固定为“用户输入如何经过会话状态、Query Loop、请求投影和 Tool Loop 到达模型”。标杆必须同时讲清交互式 REPL 与 SDK/Headless 两条输入适配路径在 `query()` 汇合，不能误写成 REPL 必经 `QueryEngine`。

单元先形成候选稿，阶段内全部候选完成后检查跨章事实、术语、依赖、重复、主题覆盖、图文一致和 Harness 回归，再统一发布。发布失败保留旧正式版本。阶段发布后由用户提供主观反馈，Codex 只据此调整未发布内容；不根据测验成绩擅自判断学习者能力。

## 15. 轻量工作流底线

以下事项被明确取消：

- 所有源码、片段、Claim、owner、段落和审查 Hash；
- 漂移检查与重哈希；
- 句子级 Claim 和 JSON 审查系统；
- 全课程证据图和自动影响传播图；
- 每个异常分支的实验矩阵；
- 为审计而复制大批源码或产生大量中间文件；
- 不影响事实、学习、实验和 Harness 契约的边缘问题阻断；
- Graphify 输出直接进入教材或事实闸门。

精简的是无教学价值的控制面，不是教材深度。核心事实仍需源码闭合，关键行为仍需实验，重要风险仍需独立审查。

中间日志、命令输出、临时缓存、草稿碎片和过程性写入不属于教材质量目标。Codex 不需要为它们建立清单、格式审查、状态跟踪或清理闸门，也不因其残留和数量阻断课程。只有中间产物会改变源码快照、正式教材、实验结论、Harness 契约、凭据安全或阶段发布结果时才处理。

## 16. 当前真实工程状态

截至本次修订，项目状态为：

- 运行环境为 Windows，不使用 WSL；
- Codex CLI 与 Claude Code CLI 已可用；
- Claude Code CLI 已通过 CC SWITCH 接入 DeepSeek API，可直接调用 DeepSeek Max；
- 本地源码快照位于 `claude-code-CLI/`；
- 官方仓库位于 `repos/`，本轮不刷新；
- Graphify `v0.9.28` 已全局安装，并为 `claude-code-CLI/` 建立纯本地图谱；
- `graphify-out/` 是可重建缓存，不进入 Git 或教材发布物；
- 项目级 Graphify MCP 只开放只读查询工具；
- 用户已经删除旧 orchestrator、旧课程设计包、旧 Harness 与旧审查系统，不得恢复；
- 默认不自动提交 Git；本次用户已明确授权更新 README、创建远程仓库并 push。仍不做重哈希或漂移修复。

当前工作不是复活旧系统。课程设计包与 M11 标杆已经用户批准；S0 的 M01-M04 与累计 H0 已于 2026-07-28 原子发布，S1 的 M05-M09 与累计 H1 已于 2026-07-29 原子发布，S2 的 M10-M15 与累计 H2 已于 2026-07-30 原子发布。M01-M09 核心机制串联复习章 I01 也已通过事实 A/B 与教学闸门并发布。S2 已闭合 durable message、两条输入路径、Query 控制、durable/query/API/wire 请求投影、真实模型流、Tool 调度/权限/结果/取消和两条快照执行路径的差异；六章共 76 张已渲染局部图、43 道两分钟面试题，事实与教学闸门全部闭合。累计 Harness 为 `0.3.0 / H2`，包含 revisioned ConversationStore、请求/能力快照、strict projection、OpenAI-compatible Provider、有界单消费者流、safe batch/exclusive barrier、固定并发上限、顺序提交、取消配对、工作区工具和 metadata-only Trace。它不提前冒充 Provider-specific SSE Runtime、完整 Context、扩展栈、多 Agent、恢复或 Sandbox。剩余课程按价值优先重排为 S3/M16-M19、S4/M20-M23、S5/M24-M27；最终在 M27 完成，不设置 S6 或 M28。

## 17. 最终验收

一份合格教材必须让学习者在不依赖答案的情况下：

1. 解释问题、机制和设计动机；
2. 画出真实调用、消息、状态和失败流程，并能利用教材中分布在认知转折处的局部图快速复习；
3. 从入口定位到决定性源码；
4. 区分快照事实、运行验证、新版行为、推断和迁移设计；
5. 用实验验证或反驳关键结论；
6. 修改并扩展机制；
7. 用 TypeScript 与 Python 复现同一行为契约；
8. 将机制持续集成到自己的 Harness；
9. 迁移到 Java/Spring、RAG 和企业生产系统；
10. 在大厂面试中用结论先行、口语化约两分钟的方式，完成从机制回答到源码追问、故障追问和系统设计追问的闭环。

最终原则：

> 教材以学习者建立完整运行心智模型为中心。源码证据服务于理解，实验服务于验证，Harness 服务于迁移；任何工具和流程都不能反过来成为教材的主角。
