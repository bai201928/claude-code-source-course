# 教材生成项目规则

## 需求权威

- `2.md` 是项目最高需求文件。
- `3.md` 是在最高需求约束下执行的工作流方案。
- 开始新的教材任务、阶段或章节，以及上下文恢复后，必须先完整读取 `2.md` 和 `3.md`。
- 权威顺序为：`2.md` -> 用户已确认决策 -> `3.md` -> 已批准课程设计包 -> 当前章节研究工作簿。

## Graphify

Graphify 只用于源码研究前的方向定位，不是教材事实来源。

- 对跨模块架构、调用路径、组件关系和源码入口问题，可以先查询 `graphify-out/graph.json`。
- `EXTRACTED` 关系仍须回到源码核对；`INFERRED` 和 `AMBIGUOUS` 只能作为待验证假说。
- 不因图谱存在而禁止直接读取或搜索源码。
- 教材事实、流程图、实验结论和面试答案不得以 Graphify 输出作为最终证据。
- 禁止自动执行 `graphify save-result`、`graphify reflect` 或 `graphify update`，避免模型答案回写后形成循环证据。
- 不安装或调用 Graphify strict、post-commit hook、watch 或自动更新流程。
- Graphify 失败、缺失或结果不足时，直接回退到 `rg`、源码阅读和可运行实验，不得阻断教材研究。

## 教材质量

- 教材必须按真实问题和认知过程组织，不按固定栏目拼装。
- 源码、运行机制、组件协作、状态变化、设计动机、实验、Harness 演进、企业迁移和面试表达应自然融入同一教学叙事。
- 信息密集的运行转折不能只依赖章首总图；应就地增加经源码核验的局部流程、时序、状态或所有权图，同时服务理解与复习。
- 面试部分以资深大厂 Agent 开发面试官视角出题，核心题提供结论先行、口语化约两分钟回答，题量由机制决定。
- 课程无最低单元数，当前 42 单元只是工作地图；可随源码研究和实际成章动态合并、拆分、移动或调整尚未发布内容，但必须同步依赖、主题覆盖、TypeScript 前置和 Harness 路线，不得借合并遗漏重要机制。
- 不影响事实正确性、初学者理解、实验有效性或 Harness 契约的问题不得阻断生成和发布。

## 文件与安全

- 自然语言提示词保存为 `.md` 文件。
- `claude-code-CLI/` 是当前只读源码证据，不得为生成教材而修改、格式化或写入缓存。
- Claude Code/DeepSeek 只能写入后续明确创建的独立审查工作区，不得修改正式教材、Harness 或源码快照。
- 不恢复用户已经删除的旧 orchestrator、课程、Harness 或审查系统。
- 不盘点、审查或清理无关最终质量的中间输出、日志、缓存和过程性写入；仅在其影响源码快照、正式产物、实验、Harness、凭据或发布时处理。
- 不在教材、日志或审查结果中回显 API Key、Token、Cookie 或其他凭据。
- 未经用户明确要求，不执行 Git commit、tag、push 或远程发布。
- 不执行 Hash、重哈希或源码漂移验证。
